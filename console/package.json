{
  "name": "console-workspace",
  "private": true,
  "workspaces": [
    "front",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace @console/backend",
    "dev:front": "npm run dev --workspace @console/front",
    "dev:backend": "npm run dev --workspace @console/backend"
  }
}
